# Jekyll configuration for GitHub Pages
title: kiyokaのブログアーカイブ
description: Archive of old blog posts
baseurl: "/blog-archive"
url: "https://kiyoka.github.io/blog-archive"

# Build settings
markdown: kramdown
theme: minima

# Exclude files from processing
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - memo.txt

# Permalink structure for blog posts
permalink: /:year/:month/:day/:title/

# Default values
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: "blog-archive"
    values:
      layout: "default"